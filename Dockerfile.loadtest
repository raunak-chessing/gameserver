FROM golang:1.26-alpine AS builder

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /loadtest cmd/loadtest/main.go

FROM alpine:3.19

COPY --from=builder /loadtest /loadtest

ENTRYPOINT ["/loadtest"]
